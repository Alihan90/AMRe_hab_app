name: Flutter CI Main

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy_apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter Environment
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Reconstruct Android Project Platform
        run: |
          flutter create --platforms=android --project-name=amre_hab_app .
          flutter pub get

      - name: Force Global Android SDK 34 via Gradle Properties
        run: |
          mkdir -p android
          echo "flutter.compileSdkVersion=34" >> android/local.properties
          echo "flutter.targetSdkVersion=34" >> android/local.properties
          echo "flutter.minSdkVersion=23" >> android/local.properties

      - name: Compile Application APK
        run: flutter build apk --release --no-tree-shake-icons
