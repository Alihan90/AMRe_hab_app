name: vit_rehab_app
description: "Професійний мобільний додаток для ранньої реабілітації у ВІТ"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Використовуємо перевірену версію fl_chart, яка ідеально працює на Android та iOS
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
